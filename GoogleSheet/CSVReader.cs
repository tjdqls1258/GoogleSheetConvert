using System;
using System.Collections.Generic;
using UnityEngine;

public class CSVReader
{
    private const string csvPath = "";
    public List<T> ReadCSV<T>(string file) where T : new()
    {
        List<T> result = new List<T>();

        return result;
    }
}
